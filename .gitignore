# Archived coursework — lecture recordings, slides, old exercises.
# Not part of the final project; kept on disk, out of git.
/archive/

# Build output and runtime data
/build/
/logs/
/data/

# The STM32 project (firmware/) has its own .gitignore for its build output.

# OS / editor junk
.DS_Store
Thumbs.db
*.swp

# Office lock files
~$*
